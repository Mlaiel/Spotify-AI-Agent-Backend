from fastapi import APIRouter

# Importez ici tous les routers de vos modules (v1, v2, websocket, etc.)
# Exemple : from app.api.v1 import some_router

router = APIRouter()

# router.include_router(v1_router, prefix="/v1")
# router.include_router(v2_router, prefix="/v2")
# router.include_router(websocket_router, prefix="/ws")

# À compléter selon la structure métier réelle
