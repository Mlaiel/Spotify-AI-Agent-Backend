# secrets.template.yaml
# Beispiel-Template für sichere Secrets-Verwaltung (HashiCorp Vault, AWS Secrets Manager, Azure Key Vault, GCP Secret Manager)
# Niemals echte Secrets im Repo speichern!

apiVersion: v1
kind: Secret
metadata:
  name: spotify-ai-agent-secrets
  namespace: backend
stringData:
  SECRET_KEY: "<REPLACE_ME>"
  POSTGRES_PASSWORD: "<REPLACE_ME>"
  REDIS_PASSWORD: "<REPLACE_ME>"
  SENTRY_DSN: "<REPLACE_ME>"
  SPOTIFY_CLIENT_SECRET: "<REPLACE_ME>"
  EMAIL_HOST_PASSWORD: "<REPLACE_ME>"
  # ... weitere Secrets ...
